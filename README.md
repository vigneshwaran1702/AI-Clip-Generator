# AI Video Editor

This project now runs as a Python/FastAPI application for uploading long videos and generating short clips with FFmpeg.

## Run

```bash
cd backend
python run.py
```

Then open http://localhost:8000/ in your browser.
